# ── Web Framework ──────────────────────────────────────────────────────────────
fastapi==0.115.5            # Async web framework — auto Swagger, Pydantic v2 native
uvicorn[standard]==0.32.1   # ASGI server that runs FastAPI

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36  # ORM — async version for non-blocking DB calls
asyncpg==0.30.0              # Async PostgreSQL driver for Supabase
aiosqlite==0.20.0            # Async SQLite driver for local development
alembic==1.14.0              # Database migration tool

# ── Configuration ──────────────────────────────────────────────────────────────
pydantic==2.7.4              # Data validation library
pydantic-settings==2.6.1     # Reads .env file into typed Python class
python-dotenv==1.0.1         # Loads .env file into environment

# ── ML & NLP ───────────────────────────────────────────────────────────────────
scikit-learn==1.4.2          # TF-IDF + Logistic Regression pipeline
numpy==1.26.4                # Numerical operations for ML
nltk==3.8.1                  # Tokenization and summarization
textblob==0.17.1             # Lexicon-based sentiment — used as hybrid signal

# ── Utilities ──────────────────────────────────────────────────────────────────
httpx==0.27.2                # Async HTTP client for web scraping (Google Play / App Store)
beautifulsoup4==4.12.3       # HTML parsing for App Store reviews
lxml==5.2.2                  # Fast HTML/XML parser used by BeautifulSoup
